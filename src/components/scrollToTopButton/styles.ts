import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: 48,
    height: 48,
    borderRadius: 50,
    backgroundColor: '#fff',
    borderWidth: 1,
    position: 'absolute',
    zIndex: 20,
    bottom: 52,
    right: 16,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    fontWeight: '700',
  },
});
