import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  hint: {
    marginVertical: 12,
    textAlign: 'center',
    lineHeight: 14,
  },
  button: {
    width: 150,
  },
});
